#mongo
spring:
  data:
    mongodb:
      database: madb   #� changer en fonction de notre base
      host: localhost
      port: 27017

      database: road-trip-test   #� changer en fonction de notre base

server:
  port: 8080

